{
  "name": "videodoctool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-only video compressor for student work. Targets the DDA delivery preset (1080p · 30 fps · H.264 MP4 · AAC).",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch --serve",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
